## Propriedades de Extensão (#3)

Defina uma classe `Rectangle` com duas propriedades `Int`: `width` e `height`. Adicione uma propriedade de extensão `isSquare` que verifica se `width` é igual a `height`.