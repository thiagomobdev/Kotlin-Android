## Funções de extensão (#1)

Implemente uma função de extensão `xmlTag(tag: String)` que envolve a `String`
que ela estende dentro da tag XML fornecida no parâmetro. (Consulte a Wikipedia para uma visão geral de XML).