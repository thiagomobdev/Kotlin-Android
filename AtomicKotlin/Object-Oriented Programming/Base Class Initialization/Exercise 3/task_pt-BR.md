## Inicialização da Classe Base (#3)

Defina uma hierarquia de classes com uma classe pai `Animal` e duas filhas: `Cat` e `Dog`. Adicione uma propriedade `sound` do tipo `String` a `Animal`. Adicione os parâmetros de construtor correspondentes às subclasses: `Cat` não define sua própria propriedade `sound`, mas apenas passa um valor `sound` para a classe pai.

Forneça diferentes valores padrão para `sound`: `???` para `Animal`, `meow` para `Cat` e `woof` para `Dog`.

O código no `main()` verifica os valores padrão usados quando você cria instâncias de classes específicas.