## Construtores complexos (#1)

Crie uma classe `Alien` com `name: String` como parâmetro do construtor. Adicione uma propriedade `val` chamada `myName` e atribua `name` à propriedade `myName` dentro da seção `init`.